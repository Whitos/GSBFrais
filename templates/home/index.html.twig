{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur GSB Frais{% endblock %}

{% block body %}
    <div class="flex items-center justify-center min-h-[calc(100vh-theme(spacing.32))]">
        <div class="bg-white p-10 rounded-xl shadow-lg transform transition-all duration-300 hover:scale-105 max-w-lg w-full mx-4 text-center">
            <h1 class="text-4xl font-semibold text-gray-800 mb-4">Bienvenue sur GSB Frais!</h1>
            <p class="text-gray-600 mb-8">Connectez-vous pour gérer vos frais de manière simple et efficace.</p>

            <div class="flex flex-col sm:flex-row justify-center gap-4">
                <a href="{{ path('app_login') }}" class="bg-blue-600 text-white py-2 px-6 rounded-full shadow-lg hover:bg-blue-700 hover:shadow-xl transition transform hover:-translate-y-1">
                    Connexion
                </a>
                <a href="{{ path('app_register') }}" class="bg-green-500 text-white py-2 px-6 rounded-full shadow-lg hover:bg-green-600 hover:shadow-xl transition transform hover:-translate-y-1">
                    Inscription
                </a>
            </div>
        </div>
    </div>
{% endblock %}