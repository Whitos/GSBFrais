{% extends 'base.html.twig' %}

{% block title %}Nouveau Utilisateur{% endblock %}

{% block body %}
    <div class="container mx-auto px-4 py-8">
        <div class="max-w-lg mx-auto">
            <h1 class="text-3xl font-bold text-gray-900 mb-8 text-center">Créer un nouvel utilisateur</h1>

            {{ include('user/_form.html.twig') }}

            <div class="mt-6 text-center">
                <a href="{{ path('app_user_index') }}" class="text-blue-500 hover:text-blue-600 font-medium">
                    Retour à la liste
                </a>
            </div>
        </div>
    </div>
{% endblock %}
