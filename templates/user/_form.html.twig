{{ form_start(form, {'attr': {'class': 'space-y-4'}}) }}
<div class="grid gap-4">
    {{ form_widget(form, {
        'attr': {'class': 'w-full p-3 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500'}
    }) }}
</div>

<div class="mt-6 text-center">
    <button class="w-full bg-blue-500 text-white py-2 rounded-md shadow hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500">
        {{ button_label|default('Save') }}
    </button>
</div>
{{ form_end(form) }}
